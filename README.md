# FluxKart Contact Identification Service

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Set up the `.env` file with your MySQL database credentials.

3. Run the server:
   ```bash
   npm start
   ```

## Endpoint

- POST /identify# BiteSpeedAsign
